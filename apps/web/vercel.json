{
  "buildCommand": "cd ../.. && pnpm install && pnpm turbo build --filter=@unifiedstay/web",
  "installCommand": "cd ../.. && pnpm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

